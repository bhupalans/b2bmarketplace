
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuth() {
      return request.auth != null;
    }

    function isRole(role) {
      return isAuth() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == role;
    }

    function isOwner(userId) {
      return isAuth() && request.auth.uid == userId;
    }

    // Internal documents - allow client to read/write for seeding
    match /internal/{docId} {
        allow read, write: if true;
    }

    // Users can only read and write their own profile.
    // Admins can read any user profile.
    // Allow creating the admin user during seeding.
    match /users/{userId} {
      allow get: if isOwner(userId) || isRole('admin');
      allow list: if isRole('admin');
      allow update: if isOwner(userId);
      allow create: if true; // Allow user creation during seeding
    }
    
    // All users (including logged-out visitors) can read categories
    // Allow write for seeding
    match /categories/{categoryId=**} {
      allow read, list: if true;
      allow write: if true; // Allow seeding
    }
    
    // Products have more complex rules
    match /products/{productId} {
      allow read, list: if true;
      allow write: if true; // Allow seeding
    }
  }
}
